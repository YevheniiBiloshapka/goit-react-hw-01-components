import styled from 'styled-components';
import {
  color,
  space,
  layout,
  flexbox,
  background,
  border,
} from 'styled-components';

export const Box = styled('div')(
  color,
  space,
  layout,
  flexbox,
  background,
  border
);
